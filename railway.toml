[build]
builder = "NIXPACKS"

[services.web]
startCommand = "npm run build && npm start"

[services.worker]
startCommand = "npx ts-node --transpile-only src/worker/index.ts"
cronSchedule = "0 3 * * *"
